site_name: arxiv2026_instruction_vectors
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_instruction_vectors
      handlers:
        python:
          options:
            docstring_style: numpy